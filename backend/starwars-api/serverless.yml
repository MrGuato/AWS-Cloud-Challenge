service: starwars-api

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

functions:
  starwarsfact:
    handler: handler.starwarsfact
    events:
      - http:
          path: starwarsfact
          method: get
          cors: true
